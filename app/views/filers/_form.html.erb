<%= form_with(model: filer) do |form| %>
  <% if filer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(filer.errors.count, "error") %> prohibited this filer from being saved:</h2>

      <ul>
        <% filer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
